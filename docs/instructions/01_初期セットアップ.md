# 指示文 01 — プロジェクト初期セットアップ

## 概要

`felia-home-web` をゼロから新規構築する。
既存ファイルはすべて削除し、Next.js 14 App Router ベースでプロジェクトを再構成する。

**作業ディレクトリ**: `C:\Users\User\projects\felia-home-web`

---

## ⚠️ 事前確認（作業開始前に必ず確認）

以下のファイル・フォルダが存在する場合、**Atsushi に確認を取ってから削除**すること。
自動削除は禁止。

```
削除対象（確認後）:
- app/ 以下の全ファイル
- components/ 以下の全ファイル
- public/ 以下のファイル（.gitkeep 以外）
- tailwind.config.ts / tailwind.config.js
- next.config.ts / next.config.js
- globals.css
```

---

## Step 1: 依存パッケージのインストール

`package.json` を確認し、以下がインストールされていない場合はインストールする。

```bash
npm install next-auth
npm install @types/node @types/react @types/react-dom --save-dev
```

`package.json` の dependencies に以下が揃っていることを確認:

```json
{
  "dependencies": {
    "next": "14.x.x",
    "react": "^18",
    "react-dom": "^18",
    "next-auth": "^4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "tailwindcss": "^3",
    "postcss": "^8",
    "autoprefixer": "^10"
  }
}
```

---

## Step 2: tailwind.config.ts の作成・更新

`tailwind.config.ts` を以下の内容で作成（既存ファイルは上書き）:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        // フェリアホーム カラーパレット
        felia: {
          green: "#5BAD52",
          "green-dark": "#4A9A41",
          "green-light": "#EBF7EA",
          text: "#333333",
          "text-sub": "#666666",
          border: "#E5E5E5",
          bg: "#FFFFFF",
          "bg-gray": "#F8F8F8",
        },
      },
      fontFamily: {
        sans: ["Noto Sans JP", "sans-serif"],
        montserrat: ["Montserrat", "sans-serif"],
      },
      maxWidth: {
        content: "1200px",
      },
      screens: {
        sp: "375px",
        tb: "768px",
        pc: "1200px",
      },
    },
  },
  plugins: [],
};

export default config;
```

---

## Step 3: app/globals.css の作成・更新

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&family=Noto+Sans+JP:wght@400;500;700&display=swap');

/* ベースリセット */
@layer base {
  html {
    font-family: 'Noto Sans JP', sans-serif;
    color: #333333;
    background-color: #ffffff;
  }

  body {
    background-color: #ffffff;
    color: #333333;
    -webkit-font-smoothing: antialiased;
  }

  * {
    box-sizing: border-box;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  img {
    max-width: 100%;
    height: auto;
  }
}

/* ユーティリティ */
@layer utilities {
  .font-montserrat {
    font-family: 'Montserrat', sans-serif;
  }

  .section-padding {
    @apply py-12 tb:py-16 pc:py-20;
  }

  .container-content {
    @apply mx-auto px-4 tb:px-6 pc:px-8;
    max-width: 1200px;
  }

  /* グリーンアクセント */
  .text-felia-green {
    color: #5BAD52;
  }

  .bg-felia-green {
    background-color: #5BAD52;
  }

  .border-felia-green {
    border-color: #5BAD52;
  }

  /* ボタン共通 */
  .btn-primary {
    @apply inline-flex items-center justify-center px-6 py-3 bg-felia-green text-white font-medium rounded hover:bg-felia-green-dark transition-colors duration-200;
    background-color: #5BAD52;
  }

  .btn-primary:hover {
    background-color: #4A9A41;
  }

  .btn-outline {
    @apply inline-flex items-center justify-center px-6 py-3 border-2 border-felia-green text-felia-green font-medium rounded hover:bg-felia-green hover:text-white transition-colors duration-200;
  }
}
```

---

## Step 4: app/layout.tsx の作成

```typescript
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: {
    default: "フェリアホーム | 東京の不動産売買・物件情報",
    template: "%s | フェリアホーム",
  },
  description:
    "フェリアホームは東京都内の不動産売買仲介会社です。厳選された戸建て・マンション・土地情報を多数ご紹介。売却査定・無料会員登録も受付中。",
  keywords: ["不動産", "東京", "売買", "物件", "マンション", "戸建て", "土地", "フェリアホーム"],
  openGraph: {
    type: "website",
    locale: "ja_JP",
    url: "https://index.felia-home.co.jp",
    siteName: "フェリアホーム",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body className="bg-white text-felia-text antialiased">
        {children}
      </body>
    </html>
  );
}
```

---

## Step 5: ディレクトリ構造の作成

以下のフォルダを作成する（ファイルは空の `.gitkeep` でよい）:

```
app/
  properties/
  members/
    register/
    login/
    mypage/
  search/
  news/
  sell/
  buy/
  about/
  faq/
  recruit/

components/
  layout/
  home/
  property/
  ui/

lib/
  agents/

public/
  areas/
  hero/

docs/
  instructions/
```

---

## Step 6: lib/api.ts の作成

Admin APIへのアクセスを集約するクライアント。
**このファイル経由以外でAdmin APIを呼ばないこと。**

```typescript
// lib/api.ts
const ADMIN_API_URL = process.env.ADMIN_API_URL || "http://localhost:3001";

async function fetchFromAdmin<T>(
  path: string,
  options?: RequestInit
): Promise<T> {
  const url = `${ADMIN_API_URL}${path}`;

  const res = await fetch(url, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options?.headers,
    },
    // Next.js のキャッシュ設定（必要に応じて変更）
    next: { revalidate: 60 }, // 60秒キャッシュ
  });

  if (!res.ok) {
    throw new Error(`Admin API Error: ${res.status} ${res.statusText} (${path})`);
  }

  return res.json();
}

// ---- 物件 ----

export async function getFeaturedProperties() {
  return fetchFromAdmin<Property[]>("/api/properties?flag=featured");
}

export async function getNewProperties() {
  return fetchFromAdmin<Property[]>("/api/properties?flag=new");
}

export async function getPropertiesByArea(area: string) {
  return fetchFromAdmin<Property[]>(`/api/properties?area=${encodeURIComponent(area)}`);
}

export async function getPropertiesByLine(line: string) {
  return fetchFromAdmin<Property[]>(`/api/properties?line=${encodeURIComponent(line)}`);
}

export async function getPropertyById(id: string) {
  return fetchFromAdmin<Property>(`/api/properties/${id}`);
}

// ---- 現地販売会 ----

export async function getOpenHouses() {
  return fetchFromAdmin<OpenHouse[]>("/api/open-houses");
}

// ---- お知らせ ----

export async function getNews(limit?: number) {
  const q = limit ? `?limit=${limit}` : "";
  return fetchFromAdmin<NewsItem[]>(`/api/news${q}`);
}

export async function getNewsById(slug: string) {
  return fetchFromAdmin<NewsItem>(`/api/news/${slug}`);
}

// ---- 会社情報 ----

export async function getCompanyInfo() {
  return fetchFromAdmin<CompanyInfo>("/api/company");
}

// ---- 型定義 ----

export interface Property {
  id: string;
  name: string;
  propertyType: string;
  address: string;
  price: number;
  area: number;
  layout: string;
  nearestStation: string;
  walkMinutes: number;
  mainImage: string;
  images: string[];
  isFeatured: boolean;
  isNew: boolean;
  isOpenHouse: boolean;
  isMembersOnly: boolean;
  openHouseDate?: string;
  description?: string;
  buildingYear?: number;
  structure?: string;
  floors?: string;
  managementFee?: number;
  repairReserve?: number;
  createdAt: string;
  updatedAt: string;
}

export interface OpenHouse {
  id: string;
  propertyId: string;
  propertyName: string;
  address: string;
  date: string;
  startTime: string;
  endTime: string;
  image?: string;
}

export interface NewsItem {
  id: string;
  slug: string;
  title: string;
  content: string;
  category: string;
  publishedAt: string;
}

export interface CompanyInfo {
  name: string;
  address: string;
  phone: string;
  hours: string;
  holiday: string;
  access: string;
  lat: number;
  lng: number;
  licenseNumber: string;
}
```

---

## Step 7: components/ui/SectionTitle.tsx の作成

全セクションで共通して使う見出しコンポーネント。

```typescript
// components/ui/SectionTitle.tsx

interface SectionTitleProps {
  en: string;         // 英字大見出し（例: "Felia Selection"）
  ja: string;         // 日本語サブタイトル（例: "厳選物件情報"）
  align?: "left" | "center";
}

export function SectionTitle({ en, ja, align = "center" }: SectionTitleProps) {
  const alignClass = align === "center" ? "text-center" : "text-left";

  return (
    <div className={`mb-8 tb:mb-12 ${alignClass}`}>
      <h2
        className="font-montserrat font-bold text-3xl tb:text-4xl pc:text-5xl tracking-wider text-felia-green"
        style={{ fontFamily: "'Montserrat', sans-serif", color: "#5BAD52" }}
      >
        {en}
      </h2>
      <p className="mt-2 text-sm tb:text-base text-gray-500 tracking-widest">
        {ja}
      </p>
      <div
        className="mt-3 mx-auto"
        style={{
          width: align === "center" ? "40px" : "40px",
          marginLeft: align === "left" ? "0" : "auto",
          height: "3px",
          backgroundColor: "#5BAD52",
        }}
      />
    </div>
  );
}
```

---

## Step 8: app/page.tsx の作成（トップページ骨格）

各セクションコンポーネントのプレースホルダーとして作成。
実装はセクションごとに後続の指示文で行う。

```typescript
// app/page.tsx
import { Suspense } from "react";

// セクションコンポーネント（後続の指示文で実装）
// import { HeroSlider } from "@/components/home/HeroSlider";
// import { FeliaSectionSelection } from "@/components/home/FeliaSectionSelection";
// ...

export default function HomePage() {
  return (
    <main className="bg-white">
      {/* 各セクションは後続の指示文で順次実装 */}
      <div className="container-content py-20">
        <p className="text-center text-gray-400">
          フェリアホーム HP — 初期セットアップ完了
        </p>
      </div>
    </main>
  );
}
```

---

## Step 9: next.config.ts の確認・更新

```typescript
// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        // Admin APIの画像ドメインを許可
        protocol: "https",
        hostname: "admin.felia-home.co.jp",
      },
      {
        // さくらVPS直接アクセス用（開発時）
        protocol: "http",
        hostname: "49.212.210.97",
      },
    ],
  },
};

export default nextConfig;
```

---

## Step 10: CLAUDE.md の配置

プロジェクトルートに `CLAUDE.md`（Claude.ai のチャットから受け取ったファイル）を配置する。
**このファイルが Claude Code の行動指針となる。**

---

## Step 11: 動作確認

```bash
npm run dev
```

- `http://localhost:3000` でページが表示されること
- コンソールエラーがないこと
- 背景が白（`#ffffff`）であること

---

## 完了条件チェックリスト

- [ ] `npm run dev` でエラーなく起動する
- [ ] `http://localhost:3000` で白背景のページが表示される
- [ ] `tailwind.config.ts` に `felia` カラーパレットが定義されている
- [ ] `lib/api.ts` が作成されている
- [ ] `components/ui/SectionTitle.tsx` が作成されている
- [ ] ディレクトリ構造（app/ components/ lib/ public/ docs/）が作成されている
- [ ] `CLAUDE.md` がプロジェクトルートに配置されている
- [ ] TypeScript のコンパイルエラーがない（`npx tsc --noEmit` でチェック）

---

## 次の指示文

`02_ヘッダー・フッター・アコーディオンメニュー.md`
